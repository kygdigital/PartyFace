# ComfyUI Boundary

ComfyUI and Comfy Cloud integration code belongs in this folder.

Browser components must call PartyFace API route handlers instead of calling Comfy Cloud directly. Keep `COMFY_API_KEY` server-side only.
